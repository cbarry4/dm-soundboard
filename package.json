{
  "name": "dm-soundboard",
  "version": "1.0.0",
  "description": "Standalone DM soundboard Android app built with Capacitor",
  "scripts": {
    "sync": "cap sync",
    "open": "cap open android",
    "build": "cap sync && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
